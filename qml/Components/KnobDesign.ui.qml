import QtQuick 2.15
import Constants 1.0

Item {
    id: root

    height: vSize
    width: vSize

    Rectangle {
        id: knobBackground

        anchors.fill: parent
        color: Colors.background3
    }
}
